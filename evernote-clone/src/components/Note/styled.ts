import styled from 'styled-components'

const ScNote = styled.div`
  height: 650px;
  background-color: rgba(164, 181, 196, 0.06);
  display: grid;
  grid-template-columns: 300px 3px auto;
  .vertical-line {
    border-left: 3px solid #a3b4c40f;
    height: 100%;
  }
`

export default ScNote