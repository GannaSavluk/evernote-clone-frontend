import styled from 'styled-components'

const ScLeftPanel = styled.div`
  max-width: 200px;
  background-color: #434b53;
  font-size: 14px;
  color: antiquewhite;
  font-weight: lighter;
  font-family: sans-serif;
  padding: 30px 20px;
  .header {
    display: flex;
    align-items: center;
    span {
      padding: 0 10px;
    }
    button {
      outline: none;
      border: 0;
      background: transparent;
    }
  }
`

export default ScLeftPanel