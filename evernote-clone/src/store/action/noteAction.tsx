import { NoteActionTypes, notesAction } from '../types/note'
import { Dispatch } from 'react'
import axios from 'axios'

export const postNoteToken = (login: string, password: string) => {
  return async (dispatch: Dispatch<notesAction>): Promise<void> => {
    try {

    } catch (e) {

    }
  }
}
